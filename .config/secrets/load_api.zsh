secrets_dir="$HOME/.config/secrets"

# [[ -r "$secrets_dir/openrouter.zsh" ]] && source "$secrets_dir/openrouter.zsh"

[[ -r "$secrets_dir/deepseek.zsh" ]] && source "$secrets_dir/deepseek.zsh"

# [[ -r "$secrets_dir/mimo.zsh" ]] && source "$secrets_dir/mimo.zsh"

unset secrets_dir